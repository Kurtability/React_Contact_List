import React from "react";

const Header = () => {
    return (
        // <div className="ui menu">
        <div className="ui center aligned container">
            <h1 style={{ fontSize: "3rem" }}>Contact List</h1>
        </div>
        // </div>
    );
};

export default Header;
